pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,tsx,mjs}"
      run: pnpm lint {staged_files}
    format:
      glob: "*.{js,ts,tsx,mjs,json,md,yml,yaml,css}"
      run: pnpm exec prettier --check {staged_files}

pre-push:
  commands:
    test:
      run: pnpm test
